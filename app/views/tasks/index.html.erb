<h1>All tasks</h1>

<ul class="list-unstyled">
  <% @tasks.each do | task | %>
    <li> <%= link_to task.title, show_path(task) %> | <%= task.details %> | <%= task.completed %> </li>
  <% end %>
</ul>

<%= link_to "Add a new task", tasks_new_path, class: "btn btn-primary offset" %>
